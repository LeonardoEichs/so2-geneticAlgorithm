/*

There will be no need for an Phenotpe interface, because in the Opt4j 2.x model,
the Phenotpe interface simply inherited from Object.
In the 3.x model it was removed and directly extended from Object.
In C++ there is no ROOT class such as Object so a simple use of interface
will do.

*/
